public class Calculator {
    public int add(String input) {
        if (input.isEmpty())
            return 0;
        else {
            int result = Integer.parseInt(input);
            return result;
        }
    }
}
